export const metadata = {
  title: "Agentic AI ? Main Capabilities",
  description: "AI assistant capabilities in Hindi",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="hi">
      <body>{children}</body>
    </html>
  );
}
